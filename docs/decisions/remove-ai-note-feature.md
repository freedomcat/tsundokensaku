# AIノート機能の撤去

日付: 2026-07-20

## 決定

Phase 4A として AIノートの保存機能を実装したが、実利用とプロダクト方針を見直し、保存・一覧・検索・管理の機能を撤去する。

AI回答や長期的な知識管理は外部ツールに委ねる。つんどけんさくの責務は、蔵書の検索・抽出・出典付き出力・CLI・HTTP API に限定する。

`export_events` は AIノートとは独立したエクスポート履歴として維持し、外部クライアントから `GET /api/export-events` で利用できる。
