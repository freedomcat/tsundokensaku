# つんどけんさく ロードマップ

最終更新: 2026-07-26

## コンセプト

- 積読を知識に変える
- 蔵書を検索可能にし、人やAIなど外部ツールが利用しやすい形で知識へアクセスできる基盤

つんどけんさくは AI 要約・ベクトル検索・RAG を内蔵しません（[README「あえて入れていないもの」](README.md#あえて入れていないもの) 参照）。AIは数ある外部ツールの一つとして利用できる想定ですが、特定のAIサービスに依存しません。Web UI・CLI・HTTP APIを通じて、人にもAIにも同じように使える検索・抽出・出典付き出力の基盤であることに徹します。

利用者の基本の流れは「**検索 → 資料に追加 → 資料机で整理 → エクスポート**」です。ロードマップはこの体験の完成を軸に、検索・資料机・CLI・HTTP APIなど、外部ツールへの橋渡しとなる基盤を整えます。

## 情報アーキテクチャ

つんどけんさくの情報アーキテクチャおよび各UI・操作用語は、以下の設計方針と名称で統一しています。なお、過去の完了済みフェーズ（Phase 1など）の記述については、実装当時の名称（「資料棚」など）を歴史的記録としてそのまま維持しています。

- **資料一覧**: 作成済みの資料を一覧表示し、選択する場所。
- **資料机**: 選択した資料に本・ページ・検索結果などを集め、並び替え・削除・構成を行う作業空間。
- **本の登録**: 検索対象となる本やPDFを登録し、インデックス作成・OCR・検索対象の管理などを行う場所。
- **資料管理**: 現在開いている資料に対する、新しい資料・名前変更・書き出し・読み込み・この資料を空にするなどの操作をまとめたメニュー。
- **資料に追加**: 検索結果から現在選択中の資料へ本・ページを追加する操作。
- **資料机で整理**: 資料へ追加したあと、資料机へ移動し、資料の構成や並び順を整理する導線。

## 開発・保守の共通方針

- 既存の公開URL、HTTP API、CLIおよび画面上の振る舞いを不用意に変更しない。
- 変更時には、対象となる振る舞いをテストで保護する。
- コードは可読性を優先し、責務・命名・制御フローを人間が追いやすい構造に保つ。
- 将来の可能性だけを理由とした過度な抽象化や細分化は避け、現在の責務が明確になる範囲で整理する。

## フェーズ別進捗

### Phase 0: 検索基盤 — 完了

「蔵書から探す」は一段落しました。

- 蔵書横断検索: PDF 本文・タイトル・Kindle 本・Scrapbox/Cosense メモ・書籍ノート（FTS5 + Sudachi 分かち書き + trigram 部分一致）
- 検索条件: AND/OR 切替（「すべての語を含む」チェックボックス）、`"フレーズ検索"`、`-除外語` / `-"フレーズ除外"`
- タイトル推定: PDF メタデータ → Scrapbox → ファイル名整形のフォールバック、OCR ノイズ除外、`refresh-titles` コマンド

### Phase 1: 資料棚・資料の永続化 — 完了（2026-07-07）

ワークスペース改め**資料棚**で、「問い（テーマ）」単位の**名前付き資料**（内部名はパック）を SQLite に永続化しました。かつての sessionStorage 揮発カートは廃止し、初回アクセス時に自動で資料へ移行されます。

- 資料の作成・切替・改名・削除（資料棚のセレクトボックス）
- 初回は資料0件から始まり、必要になったタイミングで作る（自動作成なし）
- 出典情報の基礎（追加時点のタイトル・ページ範囲・追加日時）を保存 — 後続 Phase のデータ基盤

設計詳細と残TODO: [docs/pack-design.md](docs/pack-design.md)

### Phase 2: 資料を組み立てる体験 — 完了（2026-07-20）

Phase 2 の目的は、検索結果を集めることではなく、**人やAIなど外部ツールへ渡せる資料として完成させる体験**を提供することです。検索結果を集め、並び替え、PDF / Markdown として出力するまでの一連を迷いなく完了できる状態にします。外部ツール向けの最適化より先にこの体験を完成させます。操作性は完成後も実際に使いながら継続的に改善します。

- [x] 資料へ追加 — 検索結果・PDF プレビュー（検索起点・資料棚起点の両方）・本文検索モーダル・アウトライン章選択から。資料がなければ作成 → 切替 → 追加まで一連で実行
- [x] 削除 — 本単位の削除・資料を空にする・資料ごと削除
- [x] 並び替え — 書籍カードの「上へ」「下へ」（エクスポート順に反映）
- [x] PDF / Markdown エクスポート — 資料の並び順どおりに本ごと出力
- [x] 追加先資料の明示 — 検索結果下部のバーに「追加先: 資料名（n冊）」を常時表示。プルダウンで切替・その場で新規作成もできる。PDF プレビューの追加ボタンと成功メッセージにも資料名を表示
- [x] PDF プレビューの操作整理 — ツールバーを役割別2段（上段=見る操作、下段=集める操作）に再構成。「資料に追加」を主役に、切出系はドロップダウンへ集約
- [x] 検索結果のまとめ表示をデフォルトに — 「同じ書籍をまとめる」を初期 ON にし、1冊1カードでヒットページを一括追加できる状態を標準にする
- [x] 資料作成の操作性のブラッシュアップ第1弾 — 検索画面の「全てクリア」撤去、検索起点 PDF プレビューからの追加、資料を空にした後の冊数表示同期、ホーム画面への資料カード追加、ZIP一括エクスポート、ページ範囲編集のチップ表示化、PDFモーダルのサムネイルページ選択UI、サムネイル画質の改善、同一PDFを複数の資料項目として扱う機能（設計: [docs/pack-item-identity-design.md](docs/pack-item-identity-design.md)）
- [x] 資料棚の情報設計・操作階層を整理 — UI設計と完了条件: [docs/workspace-ui-information-architecture.md](docs/workspace-ui-information-architecture.md)（責務・5グループ・書き出し方式 (profile, format)・Phase 2A〜2D）
  - [x] **Phase 2A: UI整理** — 操作のグループ化（日常操作とバックアップ・資料を空にするなどの低頻度操作の分離。資料そのものの削除は2C決定待ちのため対象外）、primary一本化、統計表示の1行サマリ化と用語統一、空状態への検索導線
  - [x] **Phase 2B: 書き出し整理** — 完了（2026-07-19） — 書き出し4方式を1つのモーダルへ統合、(profile, format) 対応表化、無効化基準の統一
  - [x] **Phase 2C: 資料一覧** — 完了（2026-07-19） — 資料一覧画面を追加し、規模（冊数・ページ数・推定トークン数）の比較とまとめて削除ができるようにする。資料棚との導線をここで決定
  - [x] **Phase 2D: ヘッダー再設計** — 完了（2026-07-19） — 資料棚ヘッダーを4つの情報階層へ再構成し、`#ws-pack-select`を主見出し相当に強調。単一の`#ws-management`へ新規作成・改名・バックアップ・空にする操作を「資料」「バックアップ」「危険な操作」の3グループで集約した。`ws-pack-delete`は資料棚から完全撤去し、資料削除は資料一覧の責務として確定。資料一覧への導線と「資料内からページを追加」「検索画面で本を探す」の2起点を常時表示し、ナビゲーション資料棚バッジをdistinct `pdf_path`による真の冊数へ統一。375px幅の横はみ出し防止レイアウトと回帰テストを追加（Python 364件、Playwright 16件通過）
  - [x] **Phase 2総点検** — 完了（2026-07-19） — Phase 2A〜2D完了後、現在の実装・テストのみを根拠にUX・情報設計・責務分離・表示の一貫性・保守性・回帰テスト・Phase 3以降への影響を確認した。判明した資料の冊数表示不整合（クライアント側`TsundokuCart.bookCount()`が項目数を返し、サーバー側のdistinct `pdf_path`冊数と不一致。同一PDFを複数項目に追加すると画面間で冊数表示が矛盾）はコミット`f60d4ba`で修正済み（`bookCount()`をdistinct `pdf_path`数に統一、`updateBadge()`も同関数を利用、冊数表記を「冊」へ統一、同一PDF複数項目・異なるPDFの回帰テストを追加。Python全364件・Playwright全16件通過）。ホーム画面の書き出し導線の残課題は次項およびPhase 3Eへ、状態メッセージ表示の不統一は将来のShould課題として引き継いだ。詳細: [docs/workspace-ui-information-architecture.md](docs/workspace-ui-information-architecture.md)
  - [x] **ページ追加・ホーム画面の設計確定** — 完了（2026-07-20） — 資料机内のページ追加導線・ホーム画面の偽リンク整理・UI操作用語の一括変更について設計方針を確定した（設計のみ。実装は本項以下の各項目で行う）。詳細: [docs/workspace-ui-information-architecture.md](docs/workspace-ui-information-architecture.md)
  - [x] **ホーム画面・UI操作用語の一括整理** — 「資料棚」→「資料机」、「設定」→「本の登録」、「管理」→「資料管理」、「資料棚で編集」→「資料机で整理」の用語変更と、ホーム画面の「PDF一式を書き出す」「MD一式を書き出す」という偽リンクを「資料机で整理する」の単一操作へ整理することを、ナビゲーション・見出し・ボタン・説明文・関連テストの表示文字列も含め同一の実装作業で一括反映する。画面単位で段階的に変更しない。詳細: [docs/workspace-ui-information-architecture.md](docs/workspace-ui-information-architecture.md)
  - [x] 資料机内の本文検索からPDFプレビューへ移動する導線 — 対象の本を選んだ後、既存のPDFプレビューモーダル（ページ番号入力・サムネイル選択・章選択）を常時表示の「PDFを開く」ボタンとして明示的に開けるようにする
  - [x] アウトラインなしPDFでも資料机内から目視でページを選択できること — BookScan等アウトラインのないPDFで、章選択に頼らずページ番号入力・サムネイル選択のみで資料机内の追加が完結することを確認する
  - [x] 関連する回帰テストの追加 — 上記のホーム画面・UI操作用語の一括整理およびアウトラインなしPDF対応に対し、Python・Playwrightで新規の振る舞い検証テストを追加する（表示文字列変更に伴う既存テストの文言修正自体は各実装作業に含み、ここでは新規の回帰テストケースを指す）
- [x] PDFページ選択UIの操作性・一覧性を改善 — 完了（2026-07-20）。サムネイルの小型化、拡大アイコンのホバー時表示、前後10ページ移動をモーダル上下双方から行えるようにする、を一体で対応

### Phase 3: AI 向けエクスポート最適化 — 完了（2026-07-19）

**利用者の到達状態**: 資料棚で組み立てた内容を作り直したり、外部 AI の制限に合わせて手作業で分けたりせず、宛先に適した形式・分量・出典を保ったまま書き出せる。

設計: [docs/ai-export-optimization-design.md](docs/ai-export-optimization-design.md) / 実装前監査: [docs/phase3c-3d-design-review.md](docs/phase3c-3d-design-review.md)

- [x] **Phase 3A: 資料の規模を事前に把握できる** — 完了（2026-07-11）
  - **利用者の到達状態**: AIへ渡す前に、資料の冊数・項目数・ページ数・おおよそのトークン数を確認できる。
  - **実装**: トークンバジェット可視化

- [x] **Phase 3B: AIに合わせた書き出し方法を選べる** — 完了（2026-07-11）
  - **利用者の到達状態**: 渡すAIに応じて最適な書き出し方法を選択できる。
  - **実装**: エクスポートプロファイル基盤

- [x] **Phase 3C: ChatGPT / Claudeへそのまま渡せる** — 完了（2026-07-12）
  - **利用者の到達状態**: 資料を作り直すことなく、ChatGPTやClaudeへそのまま渡せる形で書き出せる。
  - **実装**: Markdown分冊、出典保持、ZIPダウンロード、エクスポート履歴
  - ユーザーストーリーと設計: [docs/ai-export-optimization-design.md](docs/ai-export-optimization-design.md)
  - 履歴の確定仕様: [docs/export-events-design.md](docs/export-events-design.md)

- [x] **Phase 3D: 章単位で分割したPDFとして書き出せる** — 完了（2026-07-12）
  - **利用者の到達状態**: 丸ごと1冊のような大きな資料項目も、章などの意味のある単位に分割されたPDFとして書き出せる。NotebookLMなどソース読み込み型AIへの受け渡しが利用例。
  - **実装**: アウトラインによる章単位分割、アウトラインがない場合の連続ページ分割、章名またはpart番号とページ範囲の保持、補助的な隣接項目結合、ソース数警告、API・プレビュー・資料棚モーダルからの実行

- [x] **Phase 3総点検** — 完了（2026-07-19） — Phase 3A〜3D完了後、現在の実装・テストのみを根拠にUX・NotebookLM利用体験・Export Profile設計・AI向け出力品質・UI導線・一貫性・保守性・回帰テスト・Phase 4以降への影響を確認した。判明した「階層TOC（章・節など複数階層の目次）を持つPDFでNotebookLM向け章分割（chapterプロファイル）が親章と子節を重複出力する」不整合はコミット`f33f5ea`で修正済み（最上位levelのみを分割対象にし子階層は独立ファイルにしない、隣接章の境界ページ重複を除去、実PDF統合テストを追加。Python全366件通過）。残りのShould（Gemini向け説明、NotebookLM利用手順のZIP解凍案内、トークン概算表示の統一、AI書き出しのPlaywright E2E）はPhase 3Eへ引き継いだ。詳細: [docs/ai-export-optimization-design.md](docs/ai-export-optimization-design.md)

- [x] **Phase 3E: UX磨き込み** — 完了（2026-07-19） — 既存の一式書き出しへGemini向け説明を追加し、NotebookLMではZIPを解凍して中のPDFを個別追加する案内を追加。モーダル内の精緻なトークン表示を「約」で統一し、説明文・概算表示・PDF一式ZIPダウンロードを検証するPlaywright E2Eを3件追加した（Python全366件、Playwright全19件通過）

既存のエクスポート機能を発展させ、資料棚から人やAIなど外部ツールへの受け渡しを完成させます。

### Phase 4: AI 成果物の帰還 — 方針見直し・撤去済み

AIサービスから得た回答や整理内容（AIノート）を、つんどけんさく内に保存・検索・管理する構想として設計・実装した（Phase 4A）。実際の利用を経て、AI成果物はScrapboxなど外部の知識管理サービスへ保存する運用で十分であり、つんどけんさく内に専用の保存・検索・管理機能を持つ必要はないと判断した。

つんどけんさくの責務は、本・PDFの登録、本文検索、必要なページや引用の収集、出典付きでの抽出、Markdown・PDFなど外部AI・外部ノートサービスへ渡しやすい形式での出力に限定する。AI回答の保存、AIノートの一覧・編集・削除、AIノートの横断検索、AIノートと元資料の相互リンクなど、AI成果物を内部知識として管理する仕組みは持たない方針とする。

Phase 4Aの実装（DB・API・UI）は撤去済みである。Phase 4B・4Cは実装しない。

撤去判断: [docs/decisions/remove-ai-note-feature.md](docs/decisions/remove-ai-note-feature.md)

- [x] **Phase 4A: AIノートの取り込みと保存** — 完了（2026-07-20）。実装履歴として保持するが、現在のプロダクト方針では撤去済み（下記「AIノート機能の撤去」参照）
  - **実装当時の利用者の到達状態**: AIサービスで得た回答や整理内容を貼り付けて保存でき、必要に応じて生成元のエクスポート履歴と紐づけられる。
  - [x] **Phase 4A-1: 保存用DB層** — 完了（2026-07-20） — artifacts / artifact_sources テーブル、エクスポート履歴（export_events）の読み出し関数、AIノートの作成・一覧・詳細・削除のDB操作、冪等なスキーマ初期化・保証、出典スナップショットの複製とロールバック（PR #1）
  - [x] **Phase 4A-2: API層** — 完了（2026-07-20） — `GET /api/export-events`、`POST /api/artifacts`、`GET /api/artifacts`、`GET /api/artifacts/{id}`、`DELETE /api/artifacts/{id}`。title・body の必須/200文字上限/空白検証、export_event_id の型・存在検証、デモモードでの更新系（POST/DELETE）403（PR #4）
  - [x] **Phase 4A-3: UI層** — 完了（2026-07-20） — Markdown貼り付けによる取り込みフォーム、AIノートの一覧・詳細表示・削除操作を持つ `/artifacts` 画面、エクスポート履歴との関連付けUI（`GET /api/export-events`から選択）、ナビゲーションへの「AIノート」タブ追加、実バックエンドを通したPlaywright E2Eを追加（PR #7）
- [x] **AIノート機能の撤去** — 完了（2026-07-20）。AIノート関連のDB（artifacts / artifact_sources）・API（`/api/artifacts`系）・UI（`/artifacts`画面・ナビゲーション）・テスト・ドキュメント参照を削除した。NotebookLM向け出力・Markdownエクスポート・エクスポート履歴（export_events）は維持する。Phase 4Aの実装履歴はGit履歴とROADMAP上に残す
  - [x] 影響範囲の調査 — DB・API・UI・ナビゲーション・テスト・ドキュメント参照を洗い出した
  - [x] 設計書とROADMAPの更新 — 旧設計書を削除し、撤去判断を記録した
  - [x] DB・API・UI・テストの削除 — 空の旧テーブルだけを削除し、データ入りテーブルは保持する安全策を含めて削除した
  - [x] 全体検証 — 既存テスト全件通過とUI確認を行った
- Phase 4B: 蔵書横断検索への統合 — 中止。AIノート機能を内部管理しない方針のため実装しない
- Phase 4C: 出典への往復 — 中止。AIノート機能を内部管理しない方針のため実装しない

### Phase 5着手前: 構造改善と回帰保証

**開発者の到達状態**:
検索・資料・PDF・エクスポート・設定などの変更理由がモジュール単位で分かれ、
一つの機能変更による影響を関係する範囲へ限定できる。
また、構造変更の前後で利用者向けの振る舞いが変わっていないことを、
Python・Playwrightの自動テストで継続的に確認できる。

この作業では、ファイルの行数削減自体を目的にしない。
責務、変更理由、依存方向を整理し、既存の振る舞いを変えずに段階的に分離する。
機能追加と構造変更を同じPRへ混在させない。

**コード分割への着手条件**: 下記「横断的な品質・運用基盤」Mustの「CI での Python テスト常時実行」「Playwright テストの CI 実行」「テスト戦略の明文化」を、コード分割（`web.py`・`database.py`・フロントエンドの責務分離）の安全装置として先に整える。構造と依存関係の棚卸し・分割設計はコードを変更しないため、これらと並行して進められる。進捗の管理はMust側で行い、本節では重複したチェック項目を持たない。

- [x] **構造と依存関係の棚卸し**
  - `web.py`と`database.py`が現在持つ責務を一覧化する
  - ルーティング、業務ロジック、永続化、ファイル操作の依存方向を決める
  - 分割後の候補モジュールと移行順を設計文書へ記録する
  - この段階では本体コードを分割しない
  - 設計文書: [docs/central-file-refactoring-inventory.md](docs/central-file-refactoring-inventory.md)

- [ ] **`web.py`の責務分離**
  - 詳細設計: [中心ファイルの責務棚卸しと段階的分割設計](docs/central-file-refactoring-inventory.md)
  - R番号は実施順ではなく、設計書で定義した`web.py`の責務識別子
  - 分離対象外: R1 アプリ初期化・静的資産、R9 ルートハンドラはHTTP層として`web.py`に維持
  - [x] R2 設定・環境変数の解決 — `config.py`へ分離
  - [x] R3 パス解決・URL生成 — `paths.py`へ分離
  - [x] R4 表示整形中心の責務（検索結果整形）— `search_view.py`へ分離
  - [ ] R5 ライブラリ/統計の集計 — `books_repo.py`側へ寄せる候補（`database.py`系列へ合流、未実装）
  - [x] R6 インデックスジョブ — `index_job.py`へ分離
  - [ ] R7 ファイル入出力・取り込み — 子責務別に分離（詳細は設計書参照）
    - [x] R7-1 PDFアップロード保存 — `pdf_import_service.py`へ分離
    - [ ] R7-2 PDFディレクトリ取り込み — 詳細設計済み・未実装。設計はPR #23で`develop`へ反映済み（[docs/refactoring/r7-2-pdf-directory-import.md](docs/refactoring/r7-2-pdf-directory-import.md)参照）
    - [ ] R7-3 Scrapbox JSON保存・同期 — 詳細設計済み・未実装（[docs/refactoring/r7-3-scrapbox-sync.md](docs/refactoring/r7-3-scrapbox-sync.md)参照）
    - [ ] R7-4 PDF閲覧・変換・本文検索のHTTPオーケストレーション — 未設計・未実装（候補範囲は設計書参照）
  - [ ] R8 エクスポート業務ロジック — `export_service.py`候補（設計済み・未実装）
  - R7-1完了後の次の実装対象は未選定。R5・R7-2〜R7-4・R8の着手順は未確定
  - FastAPIのルーティング・入力検証・レスポンス生成を薄い層へ整理する
  - 検索、資料、PDFプレビュー、エクスポート、本の登録・設定の業務処理を分ける
  - 一つのPRでは一つの責務だけを移動する
  - 各段階でPython・Playwrightを全件実行する
  - 既存の公開URL・HTTP API・画面上の振る舞いは変更しない

- [ ] **`database.py`の責務分離**
  - 詳細設計: [中心ファイルの責務棚卸しと段階的分割設計](docs/central-file-refactoring-inventory.md)
  - D番号は実施順ではなく、設計書で定義した`database.py`の責務識別子。`web.py`系列とは別系列で、着手順は相互に依存しない
  - [ ] D1 レコード定義 — `records.py`候補（設計済み・未実装）
  - [ ] D2 接続管理 — 分離先未定（候補のみ）
  - [ ] D3 スキーマ初期化・保証・移行 — `schema.py`候補（設計済み・未実装）
  - [ ] D4 書籍・ページ・メモ・ノートの永続化 — `books_repo.py`候補（候補のみ）
  - [ ] D5 検索 — `search_repo.py`候補（候補のみ）
  - [ ] D6 資料（pack）と資料項目 — `packs_repo.py`候補（候補のみ）
  - [ ] D7 エクスポート履歴 — `export_events_repo.py`候補（設計済み・未実装）
  - スキーマ初期化・移行処理をCRUDから分ける
  - 書籍・インデックス、資料、エクスポート履歴をドメイン単位で分ける
  - トランザクション境界と接続管理の責務を明確にする
  - 公開関数の互換性を保ち、段階的に呼び出し先を移行する
  - スキーマを変更しない範囲はGit＋回帰テストでロールバック可能とする。スキーマ変更を伴う場合のみ、Must「データ保全とスキーマ管理の方針決定」で定めた手順を適用する

- [ ] **バックエンドのパッケージ構成見直し**
  - R7による`web.py`の責務分離と、`database.py`の責務分離がともに完了した後に実施する
  - 確定した責務境界に合わせて、関連モジュールをサブパッケージへ整理する
  - 候補: `web`、`pdf`、`search`、`exports`、`repositories`
  - `controllers`・`models`などの一般的な名称を先に当てはめず、現在の責務と依存方向に基づいて構成を決める
  - 公開URL・HTTP API・CLI・画面上の振る舞いは変更しない
  - 原則として業務ロジックを変更せず、ファイル移動とimport更新を中心とする
  - 必要な互換importは移行期間のみ旧モジュールに残す
  - PythonテストとPlaywrightテストを全件実行する

- [ ] **フロントエンドの責務分離**
  - `base.html`に集まった共通CSSと画面固有CSSを分ける
  - 共通テーマ、共通コンポーネント、資料机、PDFプレビュー、本の登録に整理する
  - JavaScriptのAPI通信・状態管理・DOM描画を必要な範囲で分ける
  - DOMに依存しない純粋ロジックには単体テストを追加する

- [ ] **構造改善後の検証と文書更新**
  - Python・PlaywrightがローカルとCIで全件通ることを確認する
  - 主要利用フローに変更がないことを確認する
  - `ARCHITECTURE.md`とディレクトリ構成説明を更新する
  - 残った集中箇所と次の改善候補を記録する

### Phase 5: 消化マップ・対比・共起

**利用者の到達状態**: 積読の活用状況や本同士の関係が見え、次に読む・比較する・深掘りする資料を見つけやすくなる。

- 消化マップ
- 対比資料
- 共起提示

いずれも利用履歴が効いてくる段階です。ただし現状は資料の削除・変更で過去の構成が消えます。Phase 3C から記録するエクスポート実績は、将来、消化マップ・共起のシグナルとして利用できる可能性があります（記録開始前の実績は復元不能）。事前調査: [docs/usage-history-discovery.md](docs/usage-history-discovery.md)

## 優先順位の要約

1. ~~**検索基盤**~~ — 完了
2. ~~**資料棚・資料の永続化**~~ — 完了（2026-07-07）
3. ~~**資料を組み立てる体験の完成**~~ — 完了（2026-07-20）
4. ~~**AI向けエクスポート最適化**~~ — 完了（2026-07-19）
5. ~~**AIノート機能の撤去**~~ — 完了（2026-07-20）
6. **構造改善と回帰保証** — 次の最優先。Phase 5へ進む前に実施
7. **CLI利用者向けドキュメント整備** — 内部構造に依存しないため、構造改善と並行して進められる
8. **アーキテクチャ・API・開発者ドキュメント整備** — 構造改善後の責務を反映
9. **消化マップ・対比・共起** — 構造改善と利用履歴の保全後に着手

## 横断的な品質・運用基盤

機能 Phase とは別に、リポジトリ全体を継続的に保守・公開していくために整備する品質・運用上の観点。個人のローカル利用を中心としつつ、デモ公開や将来の利用者拡大に応じて段階的に対応する。実装順や Phase 番号はここでは確定せず、着手時に個別に計画する。具体的な数値目標・採用ツールは、確定済みのものを除き測定・調査してから定める。

### Must（機能開発と並行して近いうちに整える）

- [ ] **CI での Python テスト常時実行** — 目的: ローカルの手動実行だけに依存しない回帰検知。現状: GitHub Actions（`ci.yml`、pull_request / push 発火の Python テストジョブ）を Phase 4A-1（PR #1）で追加し、develop へ導入済み。PR #1〜#10・develop から master へのリリース PR（PR #9）まで、複数 PR で継続的に成功を確認している。次: CI が失敗した状態ではマージしない運用を明文化し、必須チェック化を判断する。完了目安: Python テストが PR の必須チェックとして継続的に機能する状態
- [x] **Playwright テストの CI 実行** — 完了（2026-07-26）。GitHub Actionsの標準`ubuntu-latest`（Ubuntu 24.04）上で、一時BOOKS_DIR・DB_DIRを使い、同梱サンプルPDF 3冊から隔離インデックスを構築して`npm run test:ui`を`--workers=1`で実行し、Playwright 29件が成功。Chromium導入とcleanupも成功し、Python 371件も同じCI runで成功した。
- [x] **テスト戦略の明文化** — 完了（2026-07-26） — [docs/test-strategy.md](docs/test-strategy.md)に、Python 371件・Playwright 29件の各テスト層の保証範囲と責務、非保証範囲、新規テストの配置基準、回帰テストの原則、CIの隔離実行、`web.py`・`database.py`・フロントエンドの構造改善への適用方針を記録した。CIでのPythonテストは技術的に導入済みだが、必須チェック・branch protection・マージ運用は未確定のため、「CIでのPythonテスト常時実行」は未完了のまま扱う。
- [ ] **データ保全とスキーマ管理の方針決定** — 目的: 資料・エクスポート履歴など、失いたくないデータが増えるため、更新・障害時にデータを失わない手順を持つ。現状: スキーマは冪等な `CREATE TABLE IF NOT EXISTS` 方式でバージョン番号管理はない。資料データの JSON 書き出し・読み込みは実装済み。DB ファイル全体のバックアップは手動コピー頼み。次: スキーマバージョンの記録方法、アプリ更新前のバックアップ手順（自動か手動か）、破損検出時の対応、バックアップ形式の互換性方針を調査・設計する。「Phase 5着手前」の `database.py` 分離のうち、スキーマ変更を伴う範囲はここで決めた手順を前提とする（既存スキーマを変更しない範囲はGit＋回帰テストで足り、本項の完了を待たずに着手できる）。完了目安: 「更新に失敗しても直前の状態へ戻せる」手順が文書化されている状態

**中心ファイルの責務整理について**: `web.py`・`database.py`・フロントエンドの責務分離は、「Phase 5着手前: 構造改善と回帰保証」（上記フェーズ節）を正本として管理する。進捗チェックはそちらのみで行い、本リストにはチェック項目を置かない。

### Should（デモ公開や共同開発を意識した段階で対応）

- [ ] **lint・format チェック** — 現状: 未導入（Python・JS とも）。ツール選定は導入時に判断。目的は差分レビューをスタイル指摘から解放すること
- [ ] **Python 依存関係の固定** — 現状: pyproject.toml は下限指定（`>=`）のみでロックファイルなし（npm 側は package-lock.json あり）。Docker イメージ再ビルド時に依存が変わり得る。ロック方式・開発依存と本番依存の分離・更新方針を調査して決める
- [ ] **セキュリティ検証の体系化** — 現状: パストラバーサル対策（books_dir 境界の解決・保存先の `relative_to` 検証）とデモモード（`DEMO_MODE` での書き込み系無効化）は実装済みだが、これらを狙い撃ちする回帰テストは体系化されていない。役割の異なる3つの手段を組み合わせる。(1) **手動の脅威分析**: 境界突破（シンボリックリンク含む）・ZIP Slip・悪意ある/異常な PDF・巨大ファイルによる資源枯渇・PDF タイトルや資料名経由の XSS・アップロード中断時の一時ファイル・エラーメッセージでのローカルパス露出・SQLite 同時書き込みを検証候補として整理する。(2) **Python でのセキュリティ回帰テスト**: 分析で確認した防御を狙い撃ちするテストを少しずつ足し、退行を防ぐ。(3) **OWASP ZAP による Web UI・HTTP API の Passive/Active Scan**: HTTP 層の継続的な検証手段として、デモ公開前に限らず定期的に流す。ZAP で確認できるのは HTTP 層の一部であり、ファイルシステム境界や PDF 処理はコードレビューと (1)(2) で補う。デモモードで無効化すべき機能の一覧は、新機能追加のたびに監査する（Phase 4A のデモモード制約はその第1号）
- [ ] **性能測定と基準策定** — 現状: 性能目標値はない。100冊・1,000冊規模でのインデックス時間・差分インデックス・検索応答・DB サイズ・メモリ・サムネイル生成・エクスポート時間・大量データ時の UI 応答性を、まず実蔵書で測定してボトルネックを把握し、その結果から基準を定める。数値目標を先に決めない。改善の実施は別項（将来検討）とする
- [ ] **実利用者による検証** — 自動テストでは確認できない UX の検証。開発者本人以外による初回セットアップ、説明なしで「検索 → 資料へ追加 → ページ追加 → エクスポート」を完了できるか、初回インデックスの理解しやすさ、エラーメッセージの分かりやすさ、スマートフォン・小画面での操作性、実蔵書規模での利用観察。結果は README・初回導線・エラーメッセージの改善へ直接反映でき、性能改善より早い段階で価値がある。フィードバックを本ロードマップへ反映する経路もここで決める
- [ ] **UI設計基盤の整理** — 目的: 配色の拡張性・表示実装の集中への対応を、コード分割（「Phase 5着手前: 構造改善と回帰保証」）とは別軸で進める。現状: 基本的な配色（背景・パネル・文字色・アクセント色など）は `base.html` 内でCSS変数化済みだが、エラー表示など状態色の一部には直接指定が残る。また共通スタイルと画面固有スタイルが同一ファイルに集中している。Figma等での画面・部品の整理は未着手。次: Figmaに主要画面・主要操作フローを整理し、ボタン・入力欄・カード・モーダルなどの共通部品と通常・hover・disabled・error・emptyなどの状態を定義する。色を用途別のデザイントークンとして整理し、直接指定を減らす。ダークモードや複数テーマの実装は、この整理後に必要性を判断する。完了目安: 新しい画面・部品を追加する際、既存のデザイントークンと部品定義を参照して一貫した見た目にできる状態
- [ ] **ブランチ運用と PR の定着** — 現状: PR #1〜#10はすべてマージ済み。feature ブランチから develop への PR、CI 確認後のマージ、feature ブランチの削除、develop から master へのリリース PR（PR #9）まで一連の運用実績がある。GitHub Actions は feature・develop・master を対象に実行している。一方、開発正本は develop だが GitHub のデフォルトブランチは master のままで、必須チェックやブランチ保護、PR 運用ルールは未確定。次: デフォルトブランチの扱い、必須チェック（CI 緑）とブランチ保護の要否判断、PR 運用ルールの文書化、AI レビューをどの段階で必須とするかの明文化。完了目安: 変更理由・レビュー・CI結果・マージ判断が GitHub 上で一貫して追える状態

### CLI・ドキュメント整備

CLI は Web UI・HTTP API と並ぶ利用インターフェースの一つとして残している一方、README・ARCHITECTURE・scripts/README・tests/playwright/README に案内が分散している。利用者・開発者・運用者向けの案内を整理し、Web UI 中心の構成の中でも CLI の使い方と責務分担が追える状態にする。

#### 利用者向け

- [ ] **CLI利用ガイド** — 利用者がCLIから本の登録・検索・エクスポートなどを行うための基本的な使い方を案内する
- [ ] **CLIコマンド一覧** — `index` / `search` / `refresh-titles` と主要オプションを一覧化し、役割を明示する
- [ ] **実践的な利用例** — 検索、再インデックス、タイトル再推定など、日常の操作例を示す
- [ ] **WebUIとの対応表** — Web UI のどの画面・操作が CLI と対応するかに加え、CLI ならではのバッチ処理やスクリプト利用の入口も整理する
- [ ] **バッチ利用例** — シェルスクリプト・PowerShell・CI などからの連続実行例を案内する

#### 開発者向け

- [ ] **アーキテクチャ** — `ARCHITECTURE.md` を基点に、CLI / Web UI / scripts の責務境界を整理する
- [ ] **API仕様整備** — 目的: 開発者や将来のクライアント実装が、現在のAPI全体（資料・検索・エクスポート・設定など）の入力・出力・エラー・デモモード制約を一貫した仕様として確認できる。実装: OpenAPI仕様の管理方針の決定（FastAPI自動生成を正本とするか、`openapi.yaml` を管理するかの判断）、`/docs` または生成した仕様書の開発者向け整備、APIの利用方法・入力・出力・エラー・デモモード制約を確認できる状態にする
- [ ] **ディレクトリ構成** — `src/`、`docs/`、`scripts/`、`tests/` の役割と文書の置き場を整理する
- [ ] **開発フロー** — ローカル起動、インデックス作成、テスト、確認の流れを整理する
- [ ] **テスト方法** — Python / Playwright / CLI の確認方法を整理する
- [ ] **リリース手順** — バージョン管理、タグ、更新、ロールバックの手順を整理する

#### 共通

- [ ] **FAQ** — 利用者・開発者・コントリビューターからの問い合わせを前提に、よくある疑問をまとめる
- [ ] **トラブルシューティング** — インデックス失敗、PDF 読み込み失敗、CLI 実行失敗の切り分けをまとめる

### 将来検討（利用者拡大・公開運用が視野に入った段階で）

- [ ] **型チェック導入** — 現状: 型ヒントは書かれているが検証は未導入。導入コストと効果を見て判断
- [ ] **性能改善の実施** — 測定と基準策定（Should）でボトルネックが判明した箇所への対応。長時間処理の進捗表示・中断・再実行もこの段階で扱う
- [ ] **認証・アクセス制御** — 外部公開（デモを超えた常時公開）時の要否判断と導入。ローカル個人利用の現段階では対象外
- [ ] **リリース管理** — バージョニング・CHANGELOG またはリリースノート・リリースタグ・対応 DB スキーマバージョンの明記・インストール/更新/ロールバック手順・不具合報告やコントリビューション手順。ライセンス（MIT）とサンプル PDF の再配布条件（README に記載済み）は変更のたびに継続確認する
- [ ] **依存関係の脆弱性監視** — 既知脆弱性の自動チェック（Dependabot 等）。依存固定（Should）の後に載せる方が効果的

## 将来構想（Long-term Vision）

※ここでは数年単位で目指す方向性を示します。
実装時期や優先順位は未定ですが、設計上は拡張できる構成を維持します。

### マルチデバイス対応

- Google Drive などのクラウドストレージを同期先として利用できるようにする
- Windows / Linux / macOS / Android で同じ資料を扱えるようにする
- 検索インデックスは各端末でローカル保持し、高速検索を維持する
- ストレージ層を抽象化し、Google Drive 以外（OneDrive、Dropbox、WebDAV など）にも対応可能な設計とする

## やらないこと

- AI 要約・ベクトル検索・RAG の内蔵（AI は外部サービスに任せる）
- 一般的な PDF ビューア・ファイル管理ソフトの機能の追いかけ
